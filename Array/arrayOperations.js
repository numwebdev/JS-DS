const numbers = [1, 3, 8, 2, 5];
const strings = ['n', 'u', 'm'];

/**
 * Add element at the End
 */
strings.push('a');
strings.push('n');


/**
 * Remove last element
 */
strings.pop();




console.log(strings);